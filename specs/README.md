# Specs

The specification store. One spec per feature or change.

## How a spec works

1. Write it using `template.md`.
2. Ratify it at the gate for its tier.
3. The spec is the contract. If the spec changes, the work changes.

## Tiering

A spec's tier follows the task it describes. See
`governance/policy/task-tiering.md`.
