# Walkthrough

A guided trip through one change:

1. Write a spec in `specs/`.
2. Open a change. The path guard checks scope.
3. Typecheck and tests pass.
4. Tessa validates independently.
5. The owner accepts. The change merges.

Run it yourself:

```bash
npm install
npm run install:factory
npm test
```
