# 06 — Profiles

A profile is a per-stack rule pack: paths, commands, forbidden patterns.

The manifest is the per-repo source of truth. The profile is the shared
convention. A change to one stack touches one profile.

Full detail lands with M3.
