# 02 — The pipeline

One loop: signal in, change out, evidence at every step.

Signal → Triage → Plan → Build → Validate → Deploy → Observe → Signal.

Spec → change → deploy is the same loop stated as a path. The tiering policy
(`governance/policy/task-tiering.md`) decides how much ceremony each trip
through the loop carries.

Full detail lands with M3.
